# Qwen3 embedding service — env template (Zero-Trust: real values in .env.local / Secret Manager only)
EMBED_MODEL=Qwen/Qwen3-Embedding-8B
EMBED_DIM=1024
GPU_MEM_UTIL=0.85
EMBED_TOKEN=        # [secret] bearer token clients must send; in prod via Secret Manager (embed-token)
